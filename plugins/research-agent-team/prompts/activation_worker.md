# Activation Worker

Use the rendered activation prompt as the complete work contract. Mark the
activation running before starting, heartbeat or checkpoint during longer work,
and finish with either completion metadata or a concrete failure summary.
