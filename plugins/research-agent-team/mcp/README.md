# ResearchAgentTeam MCP

Stage 0 keeps MCP registration inert. The command surface is defined by the
plugin contracts and exposed first through the local Codex CLI bridge. Optional
MCP tools can be added in later stages once application services own the actual
state transitions.
